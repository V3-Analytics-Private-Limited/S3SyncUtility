#common

def get_total_upload_objects(directory:str, exclude_list:list) -> int:
    """Count the total number of objects (files and directories) in a directory."""
    """This function is used for upload file in model"""
    pass 


def get_total_downloads_objects(directory:str, exclude_list:list) -> int:
    """Count the total number of objects (files and directories) in a directory."""
    """This function is used for download file in model"""
    pass 

def format_time(seconds:int) -> str:
   """Format seconds into a string representation of time in HH:MM:SS format."""
   """This function is use for both download and upload files in model"""
   pass 
